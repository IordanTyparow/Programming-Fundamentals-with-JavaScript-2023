function smallestOfTheThreeNumbers(firstNumber, secondNumber, lastNumber) {

    let smallestNum = Math.min(firstNumber, secondNumber, lastNumber);

    console.log(smallestNum);
}

smallestOfTheThreeNumbers(2, 5, 3);